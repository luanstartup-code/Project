services:
  - type: web
    name: cineai-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - key: DATABASE_URL
        value: sqlite:///video_generator.db
      - key: FLASK_ENV
        value: production
      - key: CORS_ORIGINS
        value: https://cineai.vercel.app
      - key: FRONTEND_URL
        value: https://cineai.vercel.app